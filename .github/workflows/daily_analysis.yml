name: 每日股票分析

on:
  schedule:
    - cron: '30 14 * * 1-5'  # 北京时间 22:30（周一至周五）
  workflow_dispatch:

concurrency:
  group: stock-analysis
  cancel-in-progress: false

jobs:
  analyze:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    
    steps:
      - name: 检出代码
        uses: actions/checkout@v4
      
      - name: 设置 Python 环境
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      
      - name: 安装依赖
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: 执行股票分析
        env:
          # AI 配置（Kimi）
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          OPENAI_BASE_URL: ${{ secrets.OPENAI_BASE_URL }}
          OPENAI_MODEL: ${{ secrets.OPENAI_MODEL }}
          
          # 邮箱配置
          EMAIL_SENDER: ${{ secrets.EMAIL_SENDER }}
          EMAIL_PASSWORD: ${{ secrets.EMAIL_PASSWORD }}
          EMAIL_RECEIVERS: ${{ secrets.EMAIL_RECEIVERS }}
          EMAIL_SENDER_NAME: 'daily_stock_analysis股票分析助手'
          
          # 股票列表（必须设置！）
          STOCK_LIST: ${{ secrets.STOCK_LIST }}
          
          # 其他可选配置
          TAVILY_API_KEYS: ${{ secrets.TAVILY_API_KEYS }}
          REPORT_TYPE: 'full'
          MARKET_REVIEW_ENABLED: 'true'
          
        run: |
          echo "=========================================="
          echo "🚀 股票智能分析系统"
          echo "=========================================="
          echo "⏰ 时间: $(TZ='Asia/Shanghai' date '+%Y-%m-%d %H:%M:%S')"
          echo "📊 股票列表: $STOCK_LIST"
          echo ""
          echo "【配置检查】"
          echo "  Kimi API: $([ -n "$OPENAI_API_KEY" ] && echo '✅ 已配置' || echo '❌ 未配置')"
          echo "  Gmail: $([ -n "$EMAIL_PASSWORD" ] && echo '✅ 已配置' || echo '❌ 未配置')"
          echo "=========================================="
          
          # 检查必要配置
          if [ -z "$STOCK_LIST" ]; then
            echo "❌ 错误: STOCK_LIST 未设置，请检查 Secrets 配置"
            exit 1
          fi
          
          if [ -z "$OPENAI_API_KEY" ]; then
            echo "❌ 错误: OPENAI_API_KEY (Kimi) 未设置"
            exit 1
          fi
          
          python main.py
      
      - name: 上传分析报告
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: analysis-reports-${{ github.run_number }}
          path: |
            reports/
            logs/
          retention-days: 30
